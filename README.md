# Movie Watch List

A movie watchlist application built with **React**, using the **Context API** for state management across components — no external state libraries required.

## Features
- Add movies to a watchlist
- Remove movies from the list
- Global state shared across components via Context API (no prop drilling)

